# 사용 설명서

## SAP Excel 자동 정리

### 준비물
- Python 3.10 이상 설치
- 이 프로젝트 폴더

### 사용 순서

1. **패키지 설치** (처음 한 번만)
   ```
   pip install -r requirements.txt
   ```

2. **실행**
   ```
   python src/sap_excel_cleaner.py 엑셀파일경로
   ```

3. **결과 확인**
   - 원본 파일 옆에 `_정리완료_날짜.xlsx` 파일이 생깁니다.
